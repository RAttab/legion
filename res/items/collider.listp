(collider

 (info
  (type active)
  (config im_collider_config))

 (specs
  (lab-bits var 4)
  (lab-work var 8)
  (lab-energy var 16))

 (tape
  (work 16)
  (energy 128)
  (host item-assembly)
  (in item-brain
      item-nodule
      item-accelerator
      item-accelerator
      item-nodule
      item-accelerator
      item-accelerator
      item-nodule
      item-storage)
  (out item-collider)))
